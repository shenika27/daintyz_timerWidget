<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 5. 권한 처리 (설계 문서 5장) -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- 완료 시 앱이 직접 진동(알림 채널 대신 인앱 토글로 제어) -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- SCHEDULE_EXACT_ALARM은 선언하지 않음: 프레임은 핸들러 틱, 알람은 inexact 복구용 (설계 문서 4-1, 5장) -->

    <application
        android:name=".TimerWidgetApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:localeConfig="@xml/locale_config"
        android:theme="@style/Theme.DaintyzTimerWidget">

        <!-- 메인 앱 화면 -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 보유(창고)/상점/설정은 MainActivity 셸의 하단 탭 프래그먼트로 통합됨 -->

        <!-- 테마 상세/미리보기 화면 (Compose 캐러셀: 보유=상태 스와이프, 미보유=prev 스와이프) -->
        <activity
            android:name=".ui.DetailActivity"
            android:exported="false" />

        <!-- 위젯 재구성(설정) 진입: 꾹 누름 → 관리화면의 설정 버튼 → 앱 설정 탭 -->
        <activity
            android:name=".ui.WidgetConfigureActivity"
            android:exported="true"
            android:theme="@android:style/Theme.Translucent.NoTitleBar">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" />
            </intent-filter>
        </activity>

        <!-- 위젯 Provider -->
        <receiver
            android:name=".widget.CharacterTimerWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_info" />
        </receiver>

        <!-- 위젯 버튼/알람 액션 수신 (명시적 인텐트만 받으므로 exported=false) -->
        <receiver
            android:name=".receiver.TimerActionReceiver"
            android:exported="false" />

        <!-- 재부팅 복구 -->
        <receiver
            android:name=".receiver.BootCompletedReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- 타이머 진행/완료 추적용 포그라운드 서비스 -->
        <service
            android:name=".service.TimerForegroundService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="character_timer_animation" />
        </service>

        <!-- ScreenStateReceiver는 SCREEN_ON/OFF 제약상 서비스가 동적 등록 (매니페스트 정적 등록 불가) -->

    </application>

</manifest>
