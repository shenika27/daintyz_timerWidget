package com.daintyz.timerwidget.ui

/**
 * 앱 메인 화면.
 *
 * TODO(1차 구현):
 * - 증감 단위 자유 설정 (숫자패드 직접 입력)
 * - 현재 진행 중인 타이머 상태를 위젯과 동기화하여 표시
 * - 타이머 위/아래 배치 전환 토글 (변경 시 위젯 즉시 갱신 - 설계 문서 2-1)
 * - 스킨 선택 화면(SkinSelectActivity) 진입점
 */
class MainActivity
