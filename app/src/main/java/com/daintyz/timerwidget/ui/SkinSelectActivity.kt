package com.daintyz.timerwidget.ui

/**
 * 스킨 선택 화면 (설계 문서 6-4 참고).
 *
 * TODO(1차 구현):
 * - SkinRepository에서 전체 스킨 목록 로드
 * - SkinAvailabilityChecker로 잠금/해제 상태 표시 (1차 버전: 전부 해제로 보임)
 * - 잠금 표시 아이콘 / 구매 버튼 UI 자리는 마련하되 클릭 시 동작은 미구현
 */
class SkinSelectActivity
